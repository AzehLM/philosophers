#include "philo.h"

int	main(int ac, char **av)
{
	(void) av;
	parser(ac. av); // en sortie de ca on initialise la/les structures. (en ayant decide ce qu'on met dedans)
	// donc init data
	// init thread/mutexes
	// routine ? simulation ? pas compris encore ca
	// cleanup
	return (0);
}
