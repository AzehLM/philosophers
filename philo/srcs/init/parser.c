#include "common.h"

int	atou(const char *str) // choisir la range que je vais accepter : techniquement un unsigned. On accepte 0 pour les temps donner pour chaque instruction mais pas pour le nombre de philo
{
	unsigned int
}

int	parser(int ac, char **av)
{
	if (ac == 4 || ac == 5)
	{
		// atoi de chaque arg -> stock chaque resultat dans une structure
		// l'algo dependra de ces valeurs.
		// check erreur -> ce referer au README, partie Parsing errors
		//

	}
	else if (ac == 5)
	{
		do that;
	}
}
